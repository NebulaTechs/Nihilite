;; nihilite-module: -
;; nihilite-requires: -
;; Test fixture — loaded only via -Dnihilite.init=examples/init.clj; never auto-loaded.

(println "[examples/init] test fixture; opt in via -Dnihilite.init=examples/init.clj")
